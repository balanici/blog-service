package dev.balanici.blog.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class UserManagementConfig {

    // This class is intentionally left empty.
    // The UserDetailsService and PasswordEncoder beans are defined in the SecurityConfig class.
    // This class is here to provide a clear separation of concerns and to allow for future expansion if needed.
}
